//testing configuration of jest. can we run jest and pass this test:
//keep this as guard test?

describe('My Test Suite', () => {
  it('My Test Case', () => {
    expect(true).toEqual(true);
  });
});
